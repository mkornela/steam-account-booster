import AccountBoosterConfig from '../Interfaces/AccountBoosterConfig';

export const boosterConfig: AccountBoosterConfig = {
    machineName: "deeM's Hours",
    discord: {
        enabled: false,
        webhookChannel: null,
        webhookID: null
    }
}